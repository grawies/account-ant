package view;

//TODO find out if the following description is still accurate

/*
 *  This enum is used by the TransactionListPanel to define the type of input allowed in each column
 */
public enum SpreadSheetColumnType {
	ACCOUNTNUMBER,
	ACCOUNTNAME,
	DOUBLE,
	PLAINTEXT,
	DATE
}
